updatesEnabled: true
notifyUpdates: true
notifyExperimental: true
notifyBuildsAhead: true
printToLog: true
version: ${project.version}
build: ${BUILD_NUMBER}
date: ${buildTimestamp}
branch: ${BRANCH}
updateURL: ${VERSION_URL}
releasePageURL: ${RELEASE_URL}